blockdiag {
  node_height = 50;
  default_fontsize = 16;

  チーム分け -> 企画;
  企画 -> チーム計画;
  チーム計画 -> ペルソナの決定;
  ペルソナの決定 -> 情報設計;
  情報設計 -> 画面設計;
  画面設計 -> プロジェクトマネジメント計画;
  プロジェクトマネジメント計画 -> プロトタイプ作成;
  プロトタイプ作成 -> 実装;
  実装 -> IT津梁祭り準備;
  IT津梁祭り準備 -> IT津梁祭り実施;
  IT津梁祭り実施 -> 検証・改善;
  検証・改善 -> 最終報告;

  // ここから折り返し設定
  ペルソナの決定 -> 情報設計 [folded];
  プロトタイプ作成 -> 実装 [folded];
  検証・改善 -> 最終報告 [folded];
  // ここまで折り返し設定
}
